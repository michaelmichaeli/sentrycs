// This is a test file to check if we can import from react-router-dom correctly
import { BrowserRouter, Routes, Route } from 'react-router-dom';

// Just declare the imports to check if TypeScript recognizes them
const router = BrowserRouter;
const routes = Routes;
const route = Route;

export { router, routes, route }; 