/**
 * Design system types
 * 
 * This file contains type definitions for the design system.
 */

// Text variants
export type TextVariant = 'default' | 'muted' | 'primary';

// Text sizes
export type TextSize = 'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl';

// Font weights
export type FontWeight = 'normal' | 'medium' | 'bold';

// Font families
export type FontFamily = 'sans' | 'head';

// Button variants
export type ButtonVariant = 'default' | 'outline' | 'link';

// Button sizes
export type ButtonSize = 'sm' | 'md' | 'lg';

// Card variants
export type CardVariant = 'default' | 'flat' | 'interactive';

// Padding sizes
export type PaddingSize = 'none' | 'sm' | 'md' | 'lg';

// Border radius - matching the options available in cardVariants
export type BorderRadius = 'none' | 'sm' | 'md' | 'lg'; 