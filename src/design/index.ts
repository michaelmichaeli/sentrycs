export * from './tokens';

export * from './components';

export * from './utils';

export * from './constants';

import { 
  colors, 
  typography, 
  spacing, 
  borderRadius, 
  shadows, 
  grid, 
  animation, 
  zIndex, 
  breakpoints 
} from './tokens';

export const theme = {
  colors,
  typography,
  spacing,
  borderRadius,
  shadows,
  grid,
  animation,
  zIndex,
  breakpoints,
};

export default theme; 